//Complete Program for Circular doubly linked list with all operations : insertion,deletion and display.
#include <stdio.h>
#include <stdlib.h>

struct node{//template structure of one node. 
    int data;
    struct node *prev;
    struct node *next;
};
struct node *head;//pointer that stores the address of the first node.
struct node *tail;//pointer that stores the address of the last node.
//declaring all the functions this program provides.
void insert_beg();
void insert_end();
void delete_beg();
void delete_end();
void display();

int main(){
    int choice =0;
    int num;
    int count;
    count =0;
    while(choice!=9)
    {
        printf("\n*********Main Menu*********\n");  
        printf("\nChoose one option from the following list ...\n");  
        printf("\n===============================================\n");  
        printf("\n1.Insert in Beginning\n2.Insert at last\n3.Delete from Beginning\n4.Delete from last\n\n5.Show\n6.Exit\n");  
        printf("\nEnter your choice?\n");  
        scanf("\n%d",&choice);  
        switch(choice){
            case 1:
            printf("Enter data: ");
            scanf("%d",&num);
            insert_begin();
            break;
            case 2:
            printf("Function.");
            break;
            case 3:
            printf("Function.");
            break;
            case 4:
            printf("Function.");
            break;
            case 5:
            printf("Function.");
            break;
            case 6:
            printf("Function.");
            break;
        }
    }
    
    return 0;
}
void insert_begin(){
    
}
